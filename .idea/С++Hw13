#include <iostream>
using namespace std;

class Node {
public:
    int data;
    Node* next;
    Node(int value) : data(value), next(nullptr) {}
};

int main() {
     1) Максимальна кількість елементів може бути обмежена лише обсягом пам’яті.
    2) Індекси напряму не використовуються, лише через проходження вузлів.
    3) Останній символ у рядку "Сергій хороший хлопець" — 'ь'.
   4) Оголошено вузол зі значенням 10
 Node* head = new Node(10);
    cout << "Останній символ: " << "Сергій - хороший хлопець"[sizeof("Сергій - хороший хлопець") - 2] << endl;
    return 0;
}
